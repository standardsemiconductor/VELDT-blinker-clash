[  {  "BlackBox" :
      { "name" : "IceRgbDriver.iceRgbDriverPrim"
      , "kind" : "Declaration"
      , "type" :
"iceRgbDriverPrim
:: Signal dom Bit -- pwm_r,  ARG[0]
-> Signal dom Bit -- pwm_g,  ARG[1]
-> Signal dom Bit -- pwm_b,  ARG[2]
-> Signal dom (Bit, Bit, Bit)"
      , "template" :
"//SB_RGBA_DRV begin
wire ~GENSYM[BLUE][2];
wire ~GENSYM[GREEN][1];
wire ~GENSYM[RED][0];

SB_RGBA_DRV RGBA_DRIVER (
   .CURREN(1'b1),
   .RGBLEDEN(1'b1),
   .RGB0PWM(~ARG[2]),
   .RGB1PWM(~ARG[1]),
   .RGB2PWM(~ARG[0]),
   .RGB0(~SYM[2]),
   .RGB1(~SYM[1]),
   .RGB2(~SYM[0])
);
 
defparam
  RGBA_DRIVER.CURRENT_MODE = \"0b1\",
  RGBA_DRIVER.RGB0_CURRENT = \"0b000111\",
  RGBA_DRIVER.RGB1_CURRENT = \"0b000111\",
  RGBA_DRIVER.RGB2_CURRENT = \"0b000111\";
assign ~RESULT = {~SYM[0], ~SYM[1], ~SYM[2]};
 //SB_RGBA_DRV end"
      }
   }
]
